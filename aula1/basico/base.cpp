// compilação: c++ main.cpp -O2 -o main
// compilador: g++ ou clang++

// inclui todos os cabeçalhos de uma vez
// aumenta o tempo de compilação, que não importa na maratona
#include <bits/stdc++.h>

// evita ter que escrever std:: toda hora
using namespace std;

// é comum encurtar o nome do long long
using ll = long long;

int main () {
	// essa parte ajuda a tornar leitura/escrita mais rápidas
	// cin.tie(0) -> faz menos flushes, tornando saída mais rápida
	// sync_with_stdio: dessincroniza com printf/scanf -> NÃO PODE USÁ-LOS!
	cin.tie(0)->sync_with_stdio(0);

	/*
	 * lógica
	 * ...
	*/

	// return 0 é opcional
}

